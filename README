# sup!

`sup` is a simple tool to send notifications via cli. It also supports an option to optionally run a given command and then send the notification once the command finishes execution.

## Currently supported notification targets
- local (i.e. System notifications)
- telegram
